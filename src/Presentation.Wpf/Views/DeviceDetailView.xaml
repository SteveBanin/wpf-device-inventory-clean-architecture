<UserControl x:Class="Presentation.Wpf.Views.DeviceDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Text="Device Details" />

        <StackPanel Grid.Row="1" Margin="0,12,0,12">

            <!-- Name -->
            <TextBlock Text="Name"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"  Height="30" />
            <TextBlock Text="{Binding NameError}"
                       Foreground="#B00020"
                       Margin="0,4,0,10"
                       Visibility="{Binding NameError, Converter={StaticResource StringToVisibility}}"/>

            <!-- Serial Number -->
            <TextBlock Text="Serial Number"/>
            <TextBox Text="{Binding SerialNumber, UpdateSourceTrigger=PropertyChanged}"  Height="30"/>
            <TextBlock Text="{Binding SerialError}"
                       Foreground="#B00020"
                       Margin="0,4,0,10"
                       Visibility="{Binding SerialError, Converter={StaticResource StringToVisibility}}"/>

            <!-- Location -->
            <TextBlock Text="Location"/>
            <TextBox Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}"  Height="30"/>

            <!-- Last Service Date -->
            <TextBlock Margin="0,10,0,0" Text="Last Service Date"/>
            <DatePicker SelectedDate="{Binding LastServiceDate}" />

            <!-- Description -->
            <TextBlock Margin="0,10,0,0" Text="Description"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Height="100"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    ToolTip="{Binding SaveTooltip}"
                    ToolTipService.ShowOnDisabled="True"/>
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    IsCancel="True"/>
        </StackPanel>

    </Grid>
</UserControl>
